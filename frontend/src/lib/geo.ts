/**
 * The header the edge proxy sets, shared by the proxy and the page that reads
 * it. Kept in lib so a page never has to import from the proxy module.
 */
export const GEO_HEADER = "x-greenlight-geo";
