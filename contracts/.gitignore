# Foundry build artifacts. The leading slash anchors each pattern to this
# directory, which is what makes /out mean contracts/out.
/out
/cache
/broadcast

# forge flatten output. Not needed for verification, forge verify-contract
# submits standard JSON input with imports resolved. Kept ignored so a
# one off flatten for the explorer UI fallback never gets committed.
flat/
